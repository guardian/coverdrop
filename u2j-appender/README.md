# u2j-appender

An HTTP service that accepts user-to-covernode messages (wrapping user-to-journalist messages) and appends them to a Kinesis stream for downstream processing by the [CoverNode](../covernode/README.md).
