# npm supply-chain cooldown: refuse to install package versions
# published less than 7 days ago. Older npm versions silently ignore this.
min-release-age=7
# enforce npm 11.11.1 or higher to get the cooldown behavior
engine-strict=true
